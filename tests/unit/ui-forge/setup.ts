/**
 * tests/unit/ui-forge/setup.ts
 *
 * Global test setup for ui-forge jsdom tests.
 * Imports @testing-library/jest-dom matchers (toBeInTheDocument, etc.)
 * and makes them available globally via vitest's globals mode.
 */
import "@testing-library/jest-dom";
